name: rc_car_protocol
description: Shared RC car protocol constants, packets, and Arduino command helpers.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.8.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
