name: controller_mobile_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  rc_car_protocol:
    path: ../../packages/rc_car_protocol
  socket_io_client: ^2.0.3
  flutter_dotenv: ^5.1.0
  flutter_webrtc: ^1.3.1
  flutter_svg: ^2.2.3
  audioplayers: ^6.6.0
  flutter_gemma: ^0.13.5
  speech_to_text: ^7.0.0
  permission_handler: ^11.3.1
  path_provider: ^2.1.5
  model_viewer_plus: ^1.10.0
  webview_flutter: ^4.13.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/
